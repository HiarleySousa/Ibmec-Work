Tipos de codigos:

Python:
Aplicações na Vida Real:
Python é uma linguagem de propósito geral, amplamente usada em aplicações científicas, inteligência artificial, aprendizado de máquina, educação em ciência da computação, visão computacional, medicina, biologia e até astronomia.
Também é usado para desenvolvimento web no back-end (a parte do servidor de uma aplicação web).
Sintaxe:
Python é conhecido por sua sintaxe limpa e legibilidade.
Usa indentação para definir blocos de código.
É uma ótima escolha para lógica de programação e tarefas científicas.
TESTE CODIGO:
idade = 20

if idade >= 18:
    print("Você é maior de idade.")
else:
    print("Você é menor de idade.")


JavaScript:
Aplicações na Vida Real:
JavaScript é amplamente utilizado no desenvolvimento web.
Pode ser usado tanto no front-end (a parte da aplicação que o usuário vê e interage) quanto no back-end.
Permite criar componentes interativos e dinâmicos em sites e aplicativos.
Sintaxe:
JavaScript é uma linguagem versátil.
É usado para manipular elementos HTML e CSS.
É executado nos navegadores e permite interatividade do lado do cliente.
CODIGO
let idade = 20;

if (idade >= 18) {
    console.log("Você é maior de idade.");
} else {
    console.log("Você é menor de idade.");
}



Ruby:
Aplicações na Vida Real:
Ruby é frequentemente usado para desenvolvimento web e é a base do popular framework Ruby on Rails.
É conhecido por sua simplicidade e elegância.
Também é usado para automação de tarefas e scripting.
Sintaxe:
Ruby tem uma sintaxe flexível e expressiva.
É orienta 
CODIGO
idade = 20

if idade >= 18
  puts "Você é maior de idade."
else
  puts "Você é menor de idade."
end


Site usado para rodar os codigos:
https://vscode.dev/
